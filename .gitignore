target
build
scripts/*.exe
logs/*
.idea

# Claude Code の個人環境向け設定 — 共有する .claude/settings.json は追跡対象
.claude/settings.local.json

# ローカルの作業計画メモ (docs/done/ は実装完了分)
# 先頭 / でルート限定 — 追跡対象の crates/client/docs/ にマッチさせないため
/docs/

*.gguf
*.dll
*.lib

# llama.cpp prebuilt binaries — downloaded and SHA256-verified by
# .github/workflows/actions.yml; never committed
llama_cpu/
llama_cuda/
llama_vulkan/

# generated by cargo-make (build_installer) from the workspace version
installer/Version.iss